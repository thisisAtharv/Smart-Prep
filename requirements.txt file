# Web Interface
streamlit==1.43.2

# Database
pymongo==4.5.0
SQLAlchemy==2.0.39

# PDF Processing
PyMuPDF==1.25.4

# Data Processing
pandas==2.2.3
numpy==2.1.3

# Security and Encryption
cryptography==44.0.2
hashlib
# hashlib is part of the Python standard library, no need to specify version

# API Integration
openai==1.66.3
requests==2.32.3

# Utilities
python-dotenv==1.0.1
datetime
# datetime is part of the Python standard library, no need to specify version
base64
# base64 is part of the Python standard library, no need to specify version
io
# io is part of the Python standard library, no need to specify version
random
# random is part of the Python standard library, no need to specify version
re
# re is part of the Python standard library, no need to specify version
os
# os is part of the Python standard library, no need to specify version
json
# json is part of the Python standard library, no need to specify version
